export const NETWORK_STATE_KEY = 'network-state-key';
export const FETCH = `${NETWORK_STATE_KEY}:network:fetch`;
export const NETWORK_ERROR_GENERIC_MESSAGE = `${NETWORK_STATE_KEY}:error-generic-message`;
